name: material_stock
description: 五金起重工具本地进销存(扫码+出入库+开单+蓝牙小票打印)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^4.0.0
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  csv: ^6.0.0
  intl: ^0.19.0
  flutter_blue_plus: ^1.13.3
  esc_pos_utils: ^1.1.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
